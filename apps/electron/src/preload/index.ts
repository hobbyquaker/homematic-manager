/**
 * Skeleton of the preload script. The renderer never gets Node access; task 11 exposes the typed
 * IPC transport here through `contextBridge.exposeInMainWorld`.
 */
export const PRELOAD_API_NAME = 'homematicManager';
