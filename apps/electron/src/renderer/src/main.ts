// Skeleton renderer entry; task 7 provides the app shell, task 11 mounts it here.
const app = document.querySelector('#app');

if (app) {
    app.textContent = 'Homematic Manager 3 (scaffolding)';
}
